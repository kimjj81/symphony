# Example only. Do not commit real secrets.
# Apply a private copy, or create equivalent secrets with kubectl:
#   kubectl -n webhook-relay create secret generic github-webhook-relay-secrets \
#     --from-literal=github-webhook-secret='...' \
#     --from-literal=nats-auth-token='...'
apiVersion: v1
kind: Secret
metadata:
  name: github-webhook-relay-secrets
  namespace: webhook-relay
type: Opaque
stringData:
  github-webhook-secret: replace-with-private-github-webhook-secret
  nats-auth-token: replace-with-private-nats-token-if-enabled
